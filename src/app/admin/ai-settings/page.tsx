import AISettings from "@/presentation/components/admin/AISettings";

export const metadata = {
  title: "AI Settings | Admin | Smart Night Market",
};

export default function AISettingsPage() {
  return <AISettings />;
}
